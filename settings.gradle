rootProject.name = 'xpipe_java'

include 'api'
include 'core'
include 'beacon'
include 'extension'
include 'fxcomps'

buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath "io.codearte.gradle.nexus:gradle-nexus-staging-plugin:0.30.0"
    }
}
