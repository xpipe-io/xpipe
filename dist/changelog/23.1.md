- Show detected IP information for VM entries
- Don't add empty SSH config parent entry on search if no SSH config entries exist on a system
- Fix exception when setting security key SSH auth in some cases
- Fix public key file detection failing when key was a dotfile
- Fix WezTerm not launching when XPipe window was maximized
- Fix various NullPointers
