# Konfiguracja Xshell

Żeby używać Xshell jako terminala, możesz podłączyć go do mostka SSH XPipe. Działa to automatycznie, gdy tylko dodasz do Xshell lokalny klucz SSH mostka pod właściwą nazwą.

Jedyne, co musisz zrobić ręcznie, to dodać plik klucza prywatnego `%s` do Xshell pod stałą nazwą `%s`.